<system-interrupt reason="tool_call_cycle_detected">
You've been alternating between {{tool_names}} for {{window_size}} consecutive calls without making progress — repeating the same back-and-forth pattern instead of moving forward.

NEVER repeat that alternating pattern again this turn. Stop and reassess: pick a different approach, use a different tool, or summarize findings and yield if the task is actually complete.
</system-interrupt>
